# Работа без реальной базы данных

Проект настроен для работы **без необходимости настройки MySQL**. Используется in-memory хранилище данных.

## 🚀 Быстрый старт

### Python версия (FastAPI)

1. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

2. **Запустите сервер:**
   ```bash
   uvicorn main:app --reload
   ```

3. **Откройте в браузере:**
   ```
   http://localhost:8000/
   ```

**Готово!** База данных создается автоматически в памяти при запуске. Все данные сохраняются на время работы сервера.

### PHP версия

1. **Откройте `index_php.html` через веб-сервер с PHP**

2. **Или используйте встроенный сервер PHP:**
   ```bash
   php -S localhost:8000
   ```

3. **Откройте в браузере:**
   ```
   http://localhost:8000/index_php.html
   ```

**Готово!** Данные хранятся в сессиях PHP (в памяти).

## 📝 Что работает

✅ **Регистрация пользователей** - полностью функциональна  
✅ **Вход в систему** - с JWT токенами  
✅ **Просмотр профиля** - с реальными данными  
✅ **Просмотр мероприятий** - с примерами  
✅ **Запись на мероприятия** - работает  

## 🔄 Переключение на реальную MySQL

Если хотите использовать реальную MySQL базу данных:

### Python версия:

1. Создайте файл `.env`:
   ```env
   USE_MOCK_DB=false
   DB_HOST=localhost
   DB_NAME=rukapomoshchi
   DB_USER=root
   DB_PASS=
   DB_PORT=3306
   ```

2. Создайте базу данных MySQL и запустите `init_db.php`

### PHP версия:

Измените в `index_php.html`:
```javascript
const API_BASE = 'api.php';  // вместо api_mock.php
```

И убедитесь, что MySQL запущен и база данных создана.

## 💾 Хранение данных

### Python (in-memory):
- Данные хранятся в памяти (SQLite :memory:)
- При перезапуске сервера данные теряются
- Подходит для разработки и демонстрации

### PHP (сессии):
- Данные хранятся в сессиях PHP
- При закрытии браузера/сессии данные могут теряться
- Подходит для тестирования

## ⚠️ Важно

- **Данные не сохраняются** между перезапусками сервера
- Для продакшена рекомендуется использовать реальную БД
- Все файлы для работы с MySQL сохранены и готовы к использованию

