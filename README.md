# RukaPomoshchi — Система управления волонтерскими проектами

Проект представляет собой полнофункциональную систему для управления волонтерскими проектами, где НКО публикуют мероприятия, волонтеры записываются на события, ведется учет часов, рейтинга и выдача сертификатов.

## 🚀 Технологический стек

- **Backend**: FastAPI (Python 3.10+)
- **База данных**: MySQL
- **ORM**: SQLAlchemy 2.0
- **Аутентификация**: JWT (JSON Web Tokens)
- **Хеширование паролей**: bcrypt
- **Миграции**: Alembic
- **Тестирование**: pytest
- **Frontend**: HTML/CSS/JavaScript (SPA)

## 📋 Требования

- Python 3.10 или выше
- MySQL 5.7+ или MySQL 8.0+
- pip (менеджер пакетов Python)

## 🔧 Установка

### 1. Клонирование репозитория

```bash
cd "C:\Users\nikit\OneDrive\Документы\сайт"
```

### 2. Создание виртуального окружения (рекомендуется)

```bash
python -m venv venv
```

**Активация виртуального окружения:**

Windows:
```bash
venv\Scripts\activate
```

Linux/Mac:
```bash
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка базы данных

#### 4.1. Создание базы данных MySQL

Войдите в MySQL и выполните:

```sql
CREATE DATABASE IF NOT EXISTS rukapomoshchi CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

#### 4.2. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# База данных
DB_HOST=localhost
DB_NAME=rukapomoshchi
DB_USER=root
DB_PASS=
DB_PORT=3306

# JWT
SECRET_KEY=ej08rj4wg09dnviesr03wjg
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
```

**⚠️ ВАЖНО**: В продакшене обязательно измените `SECRET_KEY` на случайную строку!

#### 4.3. Инициализация базы данных

**Вариант 1: Использование SQL скрипта (для PHP версии)**

```bash
# Запустите init_db.php через веб-сервер или напрямую:
php init_db.php
```

**Вариант 2: Использование миграций Alembic (для Python версии)**

```bash
# Создание первой миграции
alembic revision --autogenerate -m "Initial database schema"

# Применение миграций
alembic upgrade head
```

**Вариант 3: Автоматическое создание таблиц (для разработки)**

Таблицы будут созданы автоматически при первом запуске приложения.

## 🏃 Запуск приложения

### Python версия (FastAPI)

```bash
uvicorn main:app --reload
```

Приложение будет доступно по адресу:
- **API**: http://127.0.0.1:8000
- **Swagger документация**: http://127.0.0.1:8000/docs
- **ReDoc документация**: http://127.0.0.1:8000/redoc
- **Frontend**: http://127.0.0.1:8000/

### PHP версия

Настройте веб-сервер (Apache/Nginx) для работы с PHP и откройте `index_php.html` в браузере.

## 📚 API Endpoints

### Аутентификация

- `POST /auth/register` - Регистрация нового пользователя
- `POST /auth/login` - Вход в систему (получение JWT токена)
- `GET /auth/me` - Получение информации о текущем пользователе
- `POST /auth/logout` - Выход из системы

### Мероприятия

- `GET /api/events` - Получение списка мероприятий
- `POST /api/events` - Создание мероприятия (только для администраторов)
- `GET /api/events/{event_id}` - Получение информации о мероприятии
- `POST /api/events/{event_id}/signup` - Запись на мероприятие
- `PUT /api/events/{event_id}` - Обновление мероприятия (только для администраторов)
- `DELETE /api/events/{event_id}` - Удаление мероприятия (только для администраторов)

### Роли

- `GET /api/roles` - Получение списка ролей (только для администраторов)

## 🔐 Аутентификация и авторизация

Система использует JWT (JSON Web Tokens) для аутентификации. После успешного входа через `/auth/login`, вы получите токен, который нужно передавать в заголовке `Authorization`:

```
Authorization: Bearer <ваш_токен>
```

### Роли в системе

- **admin** - Администратор (полный доступ)
- **coordinator** - Координатор НКО (может создавать мероприятия)
- **volunteer** - Волонтер (может записываться на мероприятия)

## 🧪 Тестирование

Запуск тестов:

```bash
pytest tests/ -v
```

Запуск с покрытием кода:

```bash
pytest tests/ --cov=app --cov-report=html
```

## 📁 Структура проекта

```
сайт/
├── app/
│   ├── api/              # API endpoints (роутеры)
│   │   ├── auth.py       # Аутентификация
│   │   ├── events.py     # Мероприятия
│   │   └── roles.py      # Роли
│   ├── core/             # Основные настройки
│   │   ├── config.py     # Конфигурация
│   │   └── database.py   # Подключение к БД
│   ├── exceptions/       # Обработка ошибок
│   ├── models/           # SQLAlchemy модели
│   ├── repositories/     # Репозитории для работы с БД
│   ├── schemes/          # Pydantic схемы (валидация)
│   ├── services/         # Бизнес-логика
│   ├── static/           # Статические файлы (HTML, CSS, JS)
│   └── init_db.sql      # SQL скрипт для инициализации БД
├── migrations/           # Миграции Alembic
├── tests/               # Тесты
├── alembic.ini          # Конфигурация Alembic
├── main.py              # Точка входа приложения
├── requirements.txt     # Зависимости Python
└── README.md           # Документация
```

## 🔒 Безопасность

- ✅ Пароли хешируются с использованием bcrypt
- ✅ JWT токены с истечением срока действия
- ✅ Валидация всех входных данных через Pydantic
- ✅ Защита от SQL-инъекций через SQLAlchemy ORM
- ✅ Обработка ошибок с соответствующими HTTP статусами

## 📝 Примеры использования API

### Регистрация пользователя

```javascript
const response = await fetch('/auth/register', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    name: 'Иван Иванов',
    email: 'ivan@example.com',
    password: 'securepassword123',
    city: 'Москва'
  })
});
```

### Вход в систему

```javascript
const response = await fetch('/auth/login', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    email: 'ivan@example.com',
    password: 'securepassword123'
  })
});

const { access_token } = await response.json();
```

### Получение списка мероприятий

```javascript
const response = await fetch('/api/events', {
  headers: {
    'Authorization': `Bearer ${access_token}`
  }
});
```

## 🐛 Решение проблем

### Ошибка подключения к базе данных

1. Убедитесь, что MySQL запущен
2. Проверьте настройки в `.env` файле
3. Убедитесь, что база данных создана

### Ошибки миграций

```bash
# Откат последней миграции
alembic downgrade -1

# Применение всех миграций заново
alembic upgrade head
```

## 📄 Лицензия

Этот проект создан в учебных целях.

## 👥 Автор

Проект разработан в рамках индивидуального проекта по МДК.

## 🔄 Версии

- **v0.2.0** - Рефакторинг с использованием repositories, services, schemes
- **v0.1.0** - Начальная версия

---

**Примечание**: Для получения дополнительной информации см. документацию FastAPI: https://fastapi.tiangolo.com/
