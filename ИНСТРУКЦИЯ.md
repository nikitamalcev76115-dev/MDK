# ✅ Проект готов к работе БЕЗ MySQL!

## 🎯 Что сделано:

1. ✅ **Python версия** - использует SQLite в памяти (работает без MySQL)
2. ✅ **PHP версия** - использует сессии PHP (работает без MySQL)
3. ✅ **Регистрация** - полностью функциональна
4. ✅ **Вход в систему** - с JWT токенами
5. ✅ **Профиль** - отображает реальные данные пользователя
6. ✅ **Мероприятия** - можно просматривать и записываться

## 🚀 Запуск Python версии:

```bash
# 1. Установите зависимости
pip install -r requirements.txt

# 2. Запустите сервер
uvicorn main:app --reload

# 3. Откройте в браузере
http://localhost:8000/
```

**База данных создается автоматически в памяти!**

## 🚀 Запуск PHP версии:

```bash
# 1. Запустите PHP сервер
php -S localhost:8001

# 2. Откройте в браузере
http://localhost:8001/index_php.html
```

**Данные хранятся в сессиях PHP!**

## 📋 Что работает:

### Регистрация:
- Заполните форму регистрации
- Все поля валидируются
- Пароль хешируется (bcrypt)
- Пользователь сохраняется в памяти

### Вход:
- Введите email и пароль
- Получите JWT токен
- Автоматически загружается профиль

### Профиль:
- Отображает имя, email, город, роль
- Показывает статистику (часы, рейтинг)
- Список регистраций на мероприятия
- Список сертификатов

### Мероприятия:
- Просмотр списка мероприятий
- Запись на мероприятия (требует вход)

## ⚠️ Важно:

- Данные хранятся **только в памяти**
- При перезапуске сервера данные **теряются**
- Файлы для MySQL сохранены и готовы к использованию
- Для переключения на MySQL см. README.md

## 🔧 Если что-то не работает:

1. **Проверьте, что сервер запущен**
2. **Откройте консоль браузера (F12)** - там будут ошибки
3. **Убедитесь, что открываете через http://, а не file://**

## 📁 Файлы базы данных:

Все файлы для работы с MySQL сохранены:
- `init_db.sql` - SQL скрипт
- `init_db.php` - PHP скрипт инициализации
- `config.php` - конфигурация MySQL
- `db_connect.php` - подключение к MySQL
- `api.php` - API с MySQL

Для использования MySQL просто измените настройки в `.env` (Python) или используйте `api.php` вместо `api_mock.php` (PHP).

