# 🚀 Быстрый старт - Работа БЕЗ настройки MySQL

Проект настроен для работы **без реальной базы данных MySQL**. Все данные хранятся в памяти.

## Python версия (FastAPI)

### 1. Установите зависимости:
```bash
pip install -r requirements.txt
```

### 2. Запустите сервер:
```bash
uvicorn main:app --reload
```

### 3. Откройте в браузере:
```
http://localhost:8000/
```

**Готово!** Теперь можно:
- ✅ Регистрироваться
- ✅ Входить в систему
- ✅ Просматривать профиль
- ✅ Просматривать мероприятия
- ✅ Записываться на мероприятия

## PHP версия

### 1. Запустите PHP сервер:
```bash
php -S localhost:8001
```

### 2. Откройте в браузере:
```
http://localhost:8001/index_php.html
```

**Готово!** Все функции работают через `api_mock.php` (без MySQL).

## 📝 Что работает

- ✅ Регистрация пользователей
- ✅ Вход в систему (JWT токены)
- ✅ Просмотр профиля с реальными данными
- ✅ Просмотр мероприятий
- ✅ Запись на мероприятия
- ✅ Отображение регистраций в профиле
- ✅ Отображение сертификатов

## ⚠️ Важно

- Данные хранятся **только в памяти**
- При перезапуске сервера данные **теряются**
- Для постоянного хранения используйте реальную MySQL (см. README.md)

## 🔄 Переключение на MySQL

Если хотите использовать реальную БД:

**Python:** Создайте `.env` с `USE_MOCK_DB=false`  
**PHP:** Измените `api_mock.php` на `api.php` в `index_php.html`

